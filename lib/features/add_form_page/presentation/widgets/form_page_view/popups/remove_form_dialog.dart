import 'package:flutter/material.dart';

class RemoveFormView extends StatelessWidget {
  const RemoveFormView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
